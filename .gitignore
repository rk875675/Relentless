.env
.env.*
!.env.example
node_modules/
__pycache__/
*.pyc
.expo/
get-jwt.mjs

# Audio assets — uploaded to Supabase Storage, not committed
content/audio/

# Local scratch paths for audio concat / deploy experiments
tmp_audio_deployed/

# Dev lesson-pack ingestion working dir (extracted pack assets, generated JSON)
scripts/_dev_pack/

# Loader handoff bundle (generated deliverable for the partner)
loader_handoff/

# Loader review artifacts (generated per run; not source)
loader_review_*.json

# Local lesson-pack zips staged for the loader (audio assets, not source)
_packs/
